- name: ensire nvm dir exits
  file:
    path: "{{ lookup('env', 'HOME') }}/.config/nvm"
 
- name: install nvm
  shell: curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.3/install.sh | sh
